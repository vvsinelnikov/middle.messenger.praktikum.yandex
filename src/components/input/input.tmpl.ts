const input = `
    <input name="{{name}}" class="input__field" type="{{type}}" placeholder="{{placeholder}}" minlength="{{minLength}}" maxlength="{{maxLength}}" {{required}} {{disabled}} />
    <span name="{{name}}-error" class="input__error"></span>
`;

export default input;
